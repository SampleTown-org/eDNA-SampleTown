<!--
  /samples/new was retired — the batch form now covers single-sample
  workflows too. +page.server.ts throws a redirect to /samples/batch
  before this component ever renders; the stub exists only because
  SvelteKit requires a +page.svelte alongside a +page.server.ts.
-->
<p class="text-slate-400">Redirecting to /samples/batch…</p>
